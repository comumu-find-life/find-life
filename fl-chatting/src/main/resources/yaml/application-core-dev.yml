# Spring Datasource Configuration
spring:
  sql:
    init:
      mode: always
  datasource:
    initialization-mode : always
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fl_chat?serverTimezone=Asia/Seoul
    username: root
    password: sin981023@

  logging:
    level:
      me.selvernine: DEBUG

  redis:
    host : localhost
    port : 6380

  # JPA Configuration
