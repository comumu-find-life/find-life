# Spring Datasource Configuration
spring:
  sql:
    init:
      mode: always
  data:
    mongodb:
      uri: mongodb+srv://sin1768:sinminseok12@cluster0.kngkt.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: chat
  datasource:
    initialization-mode : always
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fl_core?serverTimezone=Asia/Seoul
    username: root
    password: sin981023@

  logging:
    level:
      me.selvernine: DEBUG

  redis:
    host : localhost
    port : 6380

  # JPA Configuration
